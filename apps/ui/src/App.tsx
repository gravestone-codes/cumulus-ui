/** Placeholder shell. Real screens arrive in Phase 2+ (ResourceList/ResourceDetail per roadmap §7). */
export default function App() {
  return (
    <main style={{ padding: 32, fontFamily: 'Inter, system-ui, sans-serif' }}>
      <h1>Cumulus</h1>
      <p>
        NVUE console — UI scaffold. Backend health: <code>/api/v1/health</code>.
      </p>
    </main>
  );
}
